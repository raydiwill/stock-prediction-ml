[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "stock_prediction_ml"
version = "0.1.0"
description = "Personal portfolio project for stock price prediction using E2E ML model"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "catboost>=1.2.8",
    "fastapi>=0.117.1,<0.121",
    "feast>=0.50.0",
    "great-expectations>=1.8.1",
    "httpx>=0.28.1",
    "ipykernel>=7.0.1",
    "ipywidgets>=8.1.8",
    "lightgbm>=4.6.0",
    "matplotlib>=3.10.7",
    "mlflow>=3.6.0",
    "optuna>=4.6.0",
    "pandas>=2.3.3",
    "plotly>=6.4.0",
    "pyarrow>=21.0.0",
    "pydantic>=2.12.3",
    "pydantic-settings>=2.12.0",
    "pytest-mock>=3.15.1",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "scikit-learn>=1.7.2",
    "seaborn>=0.13.2",
    "setuptools>=80.9.0",
    "sqlalchemy>=2.0.44",
    "streamlit>=1.54.0",
    "tqdm>=4.67.1",
    "xgboost>=3.1.1",
]

[tool.uv]
reinstall-package = ["stock-prediction-ml"]

[tool.hatch.build]
sources = ["src"]

[tool.hatch.build.targets.wheel]
packages = ["src/stock_prediction_ml"]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.14.1",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = ["--import-mode=importlib"]
testpaths = ["tests"]
filterwarnings = [
    "ignore::marshmallow.warnings.ChangedInMarshmallow4Warning",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]

[tool.ruff.lint]
select = ["E","F","I","UP"]

[tool.ruff.lint.isort]
known-first-party = ["stock_prediction_ml"]
